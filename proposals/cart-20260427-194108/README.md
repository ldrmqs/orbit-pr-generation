# Orbit IaC Healer — cart (demo)

**Root cause:** The cart service (pod cart-7d9f7c44bd-ln627) is unable to establish a connection to its Redis backend, as evidenced by the persistent "System.ApplicationException: Wasn't able to connect to redis" errors surfacing as gRPC FailedPrecondition status codes across all observed log entries from 18:43 through 18:52 UTC on 2026-04-27.
**Category:** other

## Files Proposed

- `k8s/redis/deployment.yaml` — A complete Redis Deployment and ClusterIP Service manifest was generated from scratch, exposing Redis on port 6379 with the service named "redis" so that the cart service can resolve it via DNS (redis:6379) within the cluster. This resolves the root cause because the cart pod's repeated "Wasn't able to connect to redis" errors indicate the Redis workload and/or its Service were missing or misconfigured, and this manifest ensures a healthy Redis instance is reachable at the expected in-cluster hostname and port. (3 attempts)
- `k8s/redis/service.yaml` — A Kubernetes Service named `redis-cart` was created in the `demo` namespace, exposing port 6379 with a selector targeting pods labeled `app: redis-cart`. Without this Service, the cart pod has no stable DNS endpoint to resolve and connect to Redis, which directly causes the `System.ApplicationException: Wasn't able to connect to redis` errors observed in pod `cart-7d9f7c44bd-ln627`. (1 attempt)

**Cluster:** `orbit-aiops-platform-cluster`

> Generated by Orbit IaC Healer Agent