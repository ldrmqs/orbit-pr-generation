# Orbit IaC Healer — cart (demo)

**Root cause:** The cart service pod (cart-7d9f7c44bd-ln627) in the demo namespace is unable to establish a connection to its Redis backend, which serves as the cart storage dependency. All 50+ log entries from pod cart-7d9f7c44bd-ln627 consistently emit the same error: "System.ApplicationException: Wasn't able to connect to redis", with a gRPC status of FailedPrecondition, indicating the Redis instance is unreachable or not accepting connections at the time the cart service attempts to use it.
**Category:** other

## Files Proposed

- `k8s/redis/deployment.yaml` — A complete Redis Deployment and ClusterIP Service named "redis-cart" have been created in the "demo" namespace, exposing port 6379 with readiness/liveness probes, resource limits, and a data volume. This resolves the root cause by ensuring a healthy, schedulable Redis instance is running and reachable within the demo namespace under the expected "redis-cart" service DNS name that the cart service uses to establish its backend connection. (1 attempt)
- `k8s/redis/service.yaml` — A ClusterIP Kubernetes Service named `redis-cart` is created in the `demo` namespace, exposing TCP port 6379 and selecting pods with the label `app: redis-cart`. This resolves the root cause by providing a stable in-cluster DNS hostname (`redis-cart.demo.svc.cluster.local`) and network endpoint on the standard Redis port that the cart pod can resolve and connect to, eliminating the "Wasn't able to connect to redis" failure. (1 attempt)

**Cluster:** `orbit-aiops-platform-cluster`

> Generated by Orbit IaC Healer Agent