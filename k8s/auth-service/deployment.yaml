apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
spec:
  template:
    spec:
      containers:
      - name: auth-service
        readinessProbe:
          failureThreshold: 5
          # other probe fields unchanged
        livenessProbe:
          failureThreshold: 5
          # other probe fields unchanged